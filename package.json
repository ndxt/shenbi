{
  "name": "shenbi",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "build": "pnpm -r run build",
    "type-check": "pnpm -r run type-check",
    "test": "pnpm -r run test",
    "test:engine": "pnpm --filter @shenbi/engine test",
    "dev:preview": "pnpm --filter @shenbi/preview dev"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
